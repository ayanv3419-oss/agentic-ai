## Agent skills

### Issue tracker

Issues live in GitHub Issues; skills use the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five-label vocabulary with `ready-for-matrics-ai` for autonomous Matrics AI tasks. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
