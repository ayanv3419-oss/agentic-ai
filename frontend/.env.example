# Preferred — full URL of the FastAPI backend (no trailing slash).
# In local dev with Vite's proxy, you can set this to /api instead.
VITE_BACKEND_URL=https://agentic-ai-anet.onrender.com

# Legacy alias. Kept for backward compatibility — VITE_BACKEND_URL takes priority.
# VITE_API_BASE_URL=/api
